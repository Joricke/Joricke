# Research Chat System — Python Dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.100.0

# ASGI server (standard includes websockets, watchfiles for reload)
uvicorn[standard]>=0.23.0

# Async HTTP client for LLM API calls
httpx>=0.24.0

# Environment variable management
python-dotenv>=1.0.0

# Data validation (included with FastAPI)
pydantic>=2.0.0

# --- Optional: Production ---
# gunicorn>=21.0.0        # Production WSGI/ASGI server
# psycopg2-binary>=2.9.0  # PostgreSQL driver

# --- Optional: LTI Integration ---
# PyLTI1p3>=2.0.0         # LTI 1.3 library for Moodle integration
